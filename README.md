# grep_sources

```
Simplified usage:

grep_sources [-i] regex [files_and_directories]

regex:                  Regular expression to search for ("grep" syntax)
files_and_directories:  Files and directories to recursively search

Flags:
  -i: Case insensitive search

More detailed usage:  grep_sources -h
```
